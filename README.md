# دليل مشروع رسالة الحب المنبثقة (Popup Love Message)

شكراً لاستخدامك هذا المشروع! تم تحديث الهيكل ليكون أكثر احترافية وسهولة في التعديل. إليك كيفية تخصيصه:

## 📂 هيكل المشروع
- **index.html**: الصفحة الرئيسية (لا تحتاج لتعديلها كثيراً).
- **css/style.css**: ملف التصميم (الألوان، الخطوط، الأنميشن).
- **js/app.js**: ملف البرمجة (إعدادات الرسائل، السرعة، الصور).
- **assets/**: المجلد الذي يحتوي على الصور والصوت.

---

## 🛠️ كيفية التعديل

### 1. تغيير نص البداية والنهاية
لتعديل النصوص، افتح ملف `index.html`:
- **نص البداية**: ابحث عن السطر الذي يحتوي على `id="startDialog"` وعدّل النص بداخله.
- **الرسالة الأخيرة**: ابحث عن السطر الذي يحتوي على `id="finalMessage"` وعدّل النص العربي هناك.

### 2. تغيير الصور
الصور موجودة في مجلد `assets/images`.
- يمكنك إضافة صور جديدة أو استبدال الصور الموجودة (`heart.jpg`, `flower.jpg`, `love1.jpg`).
- إذا أضفت صوراً بأسماء جديدة، تأكد من تحديث القائمة في ملف `js/app.js`:

```javascript
/* js/app.js */
const CONFIG = {
    // ...
    images: [
        "heart.jpg",
        "flower.jpg",
        "your-new-image.jpg" // أضف اسم صورتك هنا
    ]
};
```

### 3. تغيير الموسيقى
- استبدل الملف الموجود في `assets/audio/music.mp3` بملفك الخاص.
- تأكد من تسمية الملف الجديد بـ `music.mp3`، أو عدّل الاسم في ملف `index.html`.

### 4. تعديل الألوان والخطوط
افتح ملف `css/style.css` وقم بتعديل المتغيرات في الأعلى:

```css
:root {
  --primary-color: #F8C7DA; /* لون الخلفية للنوافذ */
  --secondary-color: pink;  /* لون القلوب والظلال */
  --text-color: white;      /* لون النص */
  --bg-color: black;        /* لون خلفية الشاشة */
}
```

### 5. تعديل سرعة وعدد الفقاعات
افتح ملف `js/app.js` وعدّل الإعدادات في `CONFIG`:

```javascript
const CONFIG = {
    popupCount: 200,      // عدد الصور التي ستظهر
    popupInterval: 30,    // السرعة بين كل صورة وأخرى (بالمللي ثانية)
    popupDuration: 10000, // مدة بقاء الصورة قبل الاختفاء
    // ...
};
```

نتمنى أن ينال إعجابك! 💖
